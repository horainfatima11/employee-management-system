name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on:  ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: echo  "CI Building your project"
  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy
        run: echo "Deploying Application"
